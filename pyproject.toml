[project]
name = "pytest-clerk-mock"
version = "0.5.0"
description = "A pytest plugin for mocking Clerk authentication"
authors = [
    { name = "Julien Kmec", email = "me@julien.dev" },
]
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "clerk-backend-api>=7.0.0",
    "pydantic>=2.0.0",
]

[project.urls]
Repository = "https://github.com/julien777z/pytest-mock-clerk"

[project.entry-points."pytest11"]
mock_clerk = "pytest_clerk_mock.plugin"

[tool.poetry]
packages = [{include = "pytest_clerk_mock", from = "src"}]

[tool.poetry.group.dev.dependencies]
black = ">=26.5.1,<27"
pytest = "^9.0.0"
pytest-asyncio = "^1.4.0"
pytest-cov = "^7.0.0"
ruff = "^0.16.0"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]
asyncio_mode = "auto"
markers = [
    "contract: contract parity tests",
]

[tool.ruff]
line-length = 110
target-version = "py311"

[tool.black]
line-length = 110
target-version = ["py311", "py312", "py313", "py314"]

[tool.ruff.lint]
select = ["E", "F", "I"]

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
